server:
  port: 8761
eureka:
  instance:
    hostname: ecom-server     # eureka的实例主机名
  client:
    register-with-eureka: false     # 不要把自己注册到Eureka
    fetch-registry: false         # 不从eureka上获取服务注册的信息
    service-url:
      defaultZone: http://localhost:8761/eureka/